# Agent instructions

Project instructions live in `CLAUDE.md` at the repo root. Read that first.

## Next.js

The Next app is `apps/web`, and `next dev` maintains its own agent-rules block
in `apps/web/AGENTS.md`. **Read it before writing any Next code** — this is
Next 16, and its APIs, conventions and file structure differ from older
releases. The `next` package resolves from `apps/web`, not from here.
